[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "multi-agent-ops-system"
version = "0.1.0"
description = "Multi-agent operations automation system MVP"
requires-python = ">=3.10"
